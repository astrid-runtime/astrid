Live content open announces an in-flight guard before engine resolution and compaction fail-closes if an opener is already in flight while the mutation fence is held. A deterministic pause before lease registration proves the handle is not published to collected bytes. Refs #1564
