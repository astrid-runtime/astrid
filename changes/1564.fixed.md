External dual-closure trust policy: ring 0 verifies against compiled fixture
public keys and independent floors, copies a page-proven ramdisk region, and
asserts one combined boot/closure/M1 serial sequence. DETERMINISM remains FAIL.
The nested x86_64 UEFI build forces the serial curve25519 backend for the pinned
nightly toolchain on every host; this is a compile choice, not a crypto claim.
Ring 0 now consumes the no-std checked loader-range/page-proof primitive whose
overlap, unmapped, non-canonical, and overflow falsifiers execute in the
closure host tests; both ABI consumers pin the loader receipt layout at build
time.
