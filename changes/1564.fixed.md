Live content open announces an in-flight guard before engine resolution and compaction fail-closes if an opener is already in flight while the mutation fence is held. Replacing a catalog generation while an open is paused before lease registration still fail-closes live-handle compaction, and dropping the returned handle lets a later plan reclaim the retired generation. Refs #1564
