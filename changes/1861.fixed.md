The singleton-lock regression tests now tolerate only the bounded fork/exec
descriptor-inheritance window after a holder is dropped, while preserving
fail-closed behavior for non-contention errors.

Closes #1861
