Persist remembered command approvals before reporting success. Keep approvals scoped to the principal and workspace across runtime restarts; preserve temporary once/session choices and reject failed persistence or approvals for deleted and disabled principals.
