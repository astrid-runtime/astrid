Isolated native-kernel M1 workspace: TCG boot and W^X serial evidence
under the experimental emulator contract, strict ktest event validation,
and split stable/nightly clippy checks. Image determinism is reported
and remains FAIL.
Add a host-neutral, lease-free projection descriptor crate with ResourceId-bound
semantic object identity, checked revisions, schema/type references, immutable
snapshots and updates, and non-authoritative presentation labels. Serialized
presentation cannot mint a live invocation. Tracking #1564
Add a host-internal `StampedInvocation` boundary and dual-path ingress identity resolution for UID-bound or compatibility-mode capsule invocations. Stamp resolution is crate-private host attribution, not authority; recv revalidates or drops cached stamps so unspecified and rebound aliases cannot inherit a prior UID, and `HostState` cannot install a cloned stamp from outside the crate.

Add a workload-neutral compatibility reference-interpreter fixture crate
with an ephemeral ramfs, structured argv, and two-principal isolation
through the HostPrincipal stamp seam. `true` and `echo` are non-normative
falsifiers, not ABI. Tracking #1564

Add a bounded RV64I portable-machine fixture in the compatibility crate.
Synthetic guest images execute recovered instruction semantics with
principal-bound ephemeral RAM, instruction fuel, and identity checks.
This is not Linux, BusyBox, Hermes, or a public ABI. Tracking #1564

Add crate-private architectural-oracle coverage for every admitted RV64I
opcode class, including expected register/memory results and fail-closed
reserved, boundary, ownership, and fuel cases. This is fixture-local evidence,
not external ISA certification or a Linux compatibility claim. Tracking #1564
