Filesystem parent checks no longer list the owner catalog. Directory existence uses the directory marker (`describe`) or a prefix lookup that stops at the first child, and a walk caches a confirmed parent so N files in one directory are ~O(N). Hosted volume reads copy only overlapping extents instead of cloning the region map. Closes #1581
