Process projection cache cleanup now records provider stops in canonical
component order with each exact mount ID and its observed STOP acknowledgement,
then listener, lease-resource, cleanup-ledger, projection-root, cache-removal,
and completion evidence in one bounded execution. The first failure terminates
the evidence stream. Test-owned join handles remain owned through timeout and
body-panic unwinds, are aborted only when necessary, and reach terminal join
settlement before secondary cleanup failures are reported.
