Process projection cache cleanup now records typed listener, lease-resource,
cleanup-ledger, projection-root, cache-removal, and completion evidence in
production order. Test-owned join handles remain owned through timeout and
body-panic unwinds, are aborted only when necessary, and reach terminal join
settlement before secondary cleanup failures are reported.
