Native kernel checks now inherit Cargo's configured shared target directory instead of creating duplicate build output in every worktree. Nested UEFI bootloader builds use the exact sibling target and reject per-worktree fallback output.
