Native kernel checks and the QEMU harness now resolve one effective Cargo target root (including configured, relative, and absolute overrides) instead of creating duplicate build output in every worktree. Kernel, kimage, and nested UEFI artifacts use exact shared siblings; the regression rejects per-worktree fallback output and cleans only its owned temporary roots.
