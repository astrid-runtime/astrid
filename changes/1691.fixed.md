Corrected fail-closed crash-frame selector parsing in the isolated PIO/IDE
journal-media evidence harness so numeric selectors do not panic before the
first WRITE and `frame:0` through `frame:15` remain executable. Tracking #1691
