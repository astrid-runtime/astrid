Resume local capsule and distro installs from a caller-scoped durable package
identity before issuing a new daemon install request. Matching canonical
archive digests and complete package generations are skipped without consuming
the install request budget; mismatched or partial state remains fail-closed.
