issue_lease rechecks caller and viewed-principal retirement under the storage-mount mutation lock before create/bind/write/insert. The same critical section refuses when a captured directory binding disappeared or drifted, because AgentDelete clears the in-memory retirement tombstone after unlink. Drain uses the same lock after retirement, so admission either publishes before drain or publishes nothing. Failed endpoint or manifest cleanup keeps a revoked map entry, returns a diagnostic error, and AgentDelete preserves the identity alias until retry succeeds. Ordinary revoke uses the same helper. Refs #1564
