Isolated dual-closure continuation: the loader signs a kernel/bootstrap
closure and a canonical signed System Generation descriptor as separate
artifacts; ring 0 copies and verifies the descriptor after binding its exact
bytes to the table identity, while rejecting missing, swapped, stale, empty,
and cross-bound tables. TCG/emulator claim boundary unchanged. Image
determinism remains FAIL. Refs #1564

WP0 architecture freeze for the universal-application substrate and resource ownership model. Specs are architecture contracts, not landed-implementation claims. P1 amendment: emulator Stage B is not physical machine authority; privacy ceiling names leakage classes; ResourceAuthority binds resource_scope and budget. Named systems are non-normative fixtures/falsifiers; fixture ordering does not order architecture tracks. Economic suite measures scale-to-zero and first-token latency per claimed application fixture, not a definite Hermes requirement. Structural split keeps the original landing paths and moves later numbered chapters into architecture/ child files so every Markdown file stays within the 1000-line cap. Tracking #1564
Add a host-neutral, lease-free execution-provider descriptor crate with
application closures, admitted-instance descriptors, structured job argv,
opaque attachments and streams, checkpoints bound to provider and instance
identity, receipts bound to provider identity that cannot become live
handles, NullProvider, and a CapsuleAdapter without an inner escape hatch.
Tracking #1564

Bind each compatibility ramfs to an immutable HostPrincipal. The reference
interpreter uses a fresh owner-bound namespace per execution; Alice state
cannot be selected, observed, or mutated as Bob. Tracking #1564
Production compaction re-reads live content-handle roots inside the mutation fence. Opening or dropping a handle after plan capture fails closed with CompactionSnapshotChanged. Engine compact() without a live registry is unchanged. Refs #1564
Admin storage-mount requests bind AuthorizedPrincipal (trusted alias plus immutable PrincipalUid) at ingress before profile or capability lookup, then reconfirm that binding after the policy snapshot so alias replacement fails closed before lease admission. AdminRequestKind::requires_principal_identity replaces substring routing. Issue, Status, Sync, and Revoke consume one MountGrant; live leases retain the requester UID so alias equality is never ownership. Tests cannot dispatch mounts without that grant. Refs #1564
