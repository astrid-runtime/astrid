Isolated dual-closure stub: the loader signs a kernel/bootstrap closure
and a distinct empty System Generation as separate artifacts; ring 0
binds the measured identities and rejects missing, swapped, stale, and
cross-bound tables. TCG/emulator claim boundary unchanged. Image
determinism remains FAIL.
