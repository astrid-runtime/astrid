Delete the unreleased LooseBlob representation write path. Home and capsule
content now publish only through packed arena ingest, while legacy contiguous
recipes and loose locators fail closed during recovery.
