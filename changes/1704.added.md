Added an isolated q35/TCG native protection-domain lifecycle harness.
It binds an authenticated component before success evidence, executes a real
invalid-opcode guest, preserves peer and guest-register isolation, advances
slot generations on reuse, and excludes peer mappings. Lifecycle mutations
reject stale handles, active domains, and non-kernel CR3 state; stale generation
overflow allocates no address space. CR3 restore failures leave release
unfinished, while identity-tagged serial evidence and an exact host sequence
walker pin admission, guest state, cancellation, reclaim, and restart order.
Tracking #1704
