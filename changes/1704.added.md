Added an isolated q35/TCG native protection-domain lifecycle harness.
It binds an authenticated component before success evidence, executes a real
invalid-opcode guest, preserves peer and guest-register isolation, advances
slot generations on reuse, excludes the active CR3 and peer mappings, and
restores the saved CR3 exactly during reclamation. Tracking #1704
