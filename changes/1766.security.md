Added boot-local, private readiness receipts that bind one exact admitted
native-domain generation and execution-control lease to component identity.
A reserved CPL3 trap can mark readiness once; terminal transitions
invalidate it before observation. Tracking #1766
