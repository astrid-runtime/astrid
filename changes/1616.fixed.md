The durable runtime tree can be explicitly admitted into the system-owned ASTVOL1 catalog. Runtime sockets, locks, readiness/token/PID sentinels, the volume itself, and bootstrap host binaries remain POSIX-owned; packed WASM is reconstructed from `bin/<hash>.wasm` catalog content. Closes #1616
