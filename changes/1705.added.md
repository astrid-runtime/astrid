- Harden fixed-capability private IPC against partially applied receives,
  stale derivation subtrees, and parked-wait edge cases. Capability scans now
  cover every slot, user cancel yields a typed cancelled completion, scratch
  copies return data to the caller's wire buffer, and release/cancel drains
  queued work without leaving reusable parked state.

Tracking #1705
