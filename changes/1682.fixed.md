## Preserve Station provenance across local capsule handoff

Station installs now carry the exact verified source lock into Astrid's
daemon-owned install path. The handoff validates the lock and archive bytes
before persisting provenance, so a local replacement cannot silently erase the
source identity or accept substituted artifact data.

Refs #1682
