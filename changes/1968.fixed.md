Process storage projections now confirm native unmount before revoking leases or deleting the private mount root. FSKit STOP waits until the mountpoint is no longer `astridfs` and is an empty private directory; a leftover mount is unmounted by the broker using only the known projection paths. Lease revocation during projection teardown is idempotent when the lease is already gone or expired.
