`astrid mcp serve` now receives complete, principal-scoped `tools/list`
snapshots directly from the kernel. Each snapshot has a private monotonic
namespace epoch; `tools/list_changed` remains a payload-light hint and every
malformed or out-of-order hint triggers a full resnapshot. Capsule grants are
rechecked when snapshots are rebuilt and when tools execute. Closes #1406.
