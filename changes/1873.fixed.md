Fix macOS FSKit discovery and mounting by declaring a filesystem personality and placing authenticated callback sockets inside the extension's private sandbox container. Return explicit unknown timestamps so Finder can browse the mounted volume. Preserve lease authentication and other platforms' transports, and report resource-loading failures in the macOS log.

Report actual backing-filesystem capacity so Finder's copy preflight succeeds, and configure the mounted display label with `[filesystem].volume_name` (default `Astrid`). Writable administrative mounts write through to the live runtime projection so config edits survive shutdown. Restore explicit directory markers as directories, including empty directories created by Finder.

Report the hosted volume's allocated blocks as used space, with capacity derived from that allocation plus available backing space, rather than attributing the whole host disk's usage to Astrid. Display the backing volume's real creation/modification dates on the mount root.

Use the same managed callback endpoint validation in the kernel and private FSKit process service. Classify an absent extension container as extension-unavailable while retaining hard failures for permissions, aliases, and malformed sockets.
