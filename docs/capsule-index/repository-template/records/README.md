# Immutable publication records

Add one canonical JSON record per publication.  Do not edit or delete an
existing record.  The PR validator rejects same-coordinate equivocation; a
corrected publication receives a new SemVer version.  Keep artifact identity in
the tagged digest set, not in a URL or mutable release label.
